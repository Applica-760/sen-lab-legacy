# Evaluate summary/plot configuration

# 実験名を指定（必須、train/eval tenfoldと同じ名前）
experiment_name: "tenfold_integ"

metric: "majority_acc"

vary_param: "Nx"
vary_values: [200, 300, 400, 500, 600, 700]

filters:
  density: 0.5
  input_scale: 0.001
  rho: 0.9

fmt: ".3f"
dpi: 150
